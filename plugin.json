{
  "name": "agent-pipeline",
  "version": "0.4.0",
  "description": "Structured, multi-agent development pipeline — work packages run through design → critique → build → review → ship, with planner/reviewer/builder personas."
}
