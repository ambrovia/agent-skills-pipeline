{
  "name": "agent-pipeline",
  "version": "0.4.2",
  "description": "Structured, multi-agent development pipeline — work packages run through design → critique → build → review → ship, with pipeline-planner/pipeline-reviewer/pipeline-builder personas."
}
